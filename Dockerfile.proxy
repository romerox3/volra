FROM golang:1.25-alpine AS builder
WORKDIR /build
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /volra-proxy ./cmd/volra-proxy

FROM alpine:3.21
COPY --from=builder /volra-proxy /usr/local/bin/volra-proxy
EXPOSE 80
ENTRYPOINT ["/volra-proxy"]
