version: 1
name: my-agent
framework: generic
port: 8000
health_path: /health
dockerfile: auto
build:
  setup_commands:
    - python -m nltk.downloader punkt
    - python -c "import transformers"
  cache_dirs:
    - /root/nltk_data
    - /root/.cache/huggingface
