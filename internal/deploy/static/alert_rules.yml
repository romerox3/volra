# Generated by Volra — regenerate with: volra deploy
groups:
  - name: volra-agent
    rules:
      - alert: AgentDown
        expr: probe_success{job="agent-health"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Agent is down"
          description: "Agent health probe has been failing for more than 1 minute."
